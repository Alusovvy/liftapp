{
  "name": "liftwise",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local-first workout log and rule-based training insights.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "check": "node --check src/app.js && node --check src/domain.js && node --check src/ui/import-chooser.js && vitest run && vite build"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "4.1.10",
    "jsdom": "30.0.1",
    "vite": "8.2.0",
    "vite-plugin-pwa": "1.3.0",
    "vitest": "4.1.10"
  }
}
