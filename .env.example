# Copy this file to .env and fill in a real secret before running
# `docker compose up`. .env is gitignored — never commit the real one.
#
# Generate a value with: openssl rand -base64 48
SESSION_SECRET=replace-with-the-output-of-openssl-rand--base64-48
